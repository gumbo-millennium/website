# vim: set ft=dockerfile :
# Configuration file for Docker web

# Based on MySQL 8
FROM mysql:8

# Downgrade encrpytion, since PHP can't handle it
COPY use-weak-encryption.cnf /etc/mysql/conf.d/use-weak-encryption.cnf
